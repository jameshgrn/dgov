# `dgov compile <plan-root>` — walks tree, validates, bundles SOPs, writes compiled plan.

[tasks.compile-cmd]
summary = "Add `dgov compile` CLI — wires walker→merger→resolver→validator→sop-bundler."
prompt = """
Add `compile` command to src/dgov/cli/__init__.py:

Usage: dgov compile <plan-root-dir>

Steps:
1. walk_tree(plan_root) → PlanTree
2. merge(PlanTree) → FlatPlan
3. resolve_refs(FlatPlan) → ResolvedPlan
4. validate(ResolvedPlan) → ValidationReport  [hard-fail on cycles/conflicts/bad refs]
5. bundle_sops(ResolvedPlan, .dgov/sops/) → CompiledPlan
   - Governor picks SOPs per unit (one LLM call sees all units + all SOPs)
   - Each unit gets a merged system_prompt = base + concat(selected SOPs) + task prompt
6. Emit compiled plan to `.dgov/plans/<name>/_compiled.toml` (flat, dispatch-ready)
7. Print summary: N units, M edges, SOP mapping summary

Exit codes:
- 0: clean compile
- 1: structural error (cycle, conflict, bad ref)

Support --json flag for machine-readable output.
"""
commit_message = "Add `dgov compile` CLI command"
depends_on = [
  "compile/pipeline.validator",
  "compile/pipeline.sop-bundler",
]
files.edit = ["src/dgov/cli/__init__.py"]
files.create = ["tests/test_cli_compile.py"]
