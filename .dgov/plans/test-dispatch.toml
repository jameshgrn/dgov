[dag]
name = "Test Single Task Dispatch"
max_concurrent = 1

[tasks.test]
summary = "Test that dispatch works"
agent = "kimi-k25-0"
prompt = """
Create a simple file at .dgov/worktrees/test-task/.test-marker with content "Hello from test task".
Then commit the file.
"""
commit_message = "Test dispatch marker file"
files.create = [".test-marker"]
