[plan]
version = 1
name = "headless-smoke-test"
description = "Verify the Headless Worker Protocol and Journaling."
goal = "Verify headless worker functionality"
project_root = "."
session_root = "."

[[evals]]
id = "eval-smoke"
kind = "smoke"
statement = "File is created"
evidence = "cat SMOKE_DONE.txt"

[units.smoke-task]
summary = "Create a sentinel file headlessly"
prompt = "Create a file named 'SMOKE_DONE.txt' with the content 'HEADELESS_WORKS', then call done."
commit_message = "feat: add headless smoke sentinel"
agent = "accounts/fireworks/routers/kimi-k2p5-turbo"
satisfies = ["eval-smoke"]
files.create = ["SMOKE_DONE.txt"]
