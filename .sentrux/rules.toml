# Sentrux rules for dgov — drift detection + architectural boundaries
#
# Rules reflect actual state. Add new rules only after verification.

[structure]
# Current max depth is 7 (deepest: dgov.review_executor)
max_depth = 7

# Currently 0 cycles — keep it that way
max_cycles = 0

[metrics]
# God files: 23 currently. Prevents growth, not cleanup.
god_file_limit = 23

# Coupling: keep below 0.25 (current: 0.23)
max_coupling = 0.25

# Quality signal floor (current: 7104) — must not decrease
min_quality_signal = 7000

# Boundary rules (enforced by test, not sentrux plugin):
# - kernel.py → only actions.py, types.py (pure, no I/O)
# - worker.py → no dgov imports (standalone subprocess)
# - settlement.py → no dgov imports (pure subprocess calls)
# See tests/test_boundaries.py for enforcement.
