[plan]
version = 1
name = "decompose-lifecycle-god-file"
goal = "Extract create.py, cleanup.py, resume.py from lifecycle/_lifecycle.py"
session_root = "/Users/jakegearon/projects/dgov"

[[evals]]
id = "E1"
kind = "regression"
statement = "lifecycle public API still works"
evidence = "uv run python3 -c 'from dgov.lifecycle import create_worker_pane, close_worker_pane, resume_worker_pane; print(\"OK\")'"

[[evals]]
id = "E2"
kind = "happy_path"
statement = "create.py exists and exports creation functions"
evidence = "test -f src/dgov/lifecycle/create.py"

[[evals]]
id = "E3"
kind = "happy_path"
statement = "cleanup.py exists and exports cleanup functions"
evidence = "test -f src/dgov/lifecycle/cleanup.py"

[[evals]]
id = "E4"
kind = "happy_path"
statement = "resume.py exists and exports resume function"
evidence = "test -f src/dgov/lifecycle/resume.py"

[[evals]]
id = "E5"
kind = "happy_path"
statement = "_lifecycle.py deleted after decomposition"
evidence = "! test -f src/dgov/lifecycle/_lifecycle.py"

[[evals]]
id = "E6"
kind = "regression"
statement = "lifecycle tests pass"
evidence = "uv run pytest tests/test_lifecycle.py -v -m unit"

[units.lifecycle-extract-create]
summary = "Extract create.py from lifecycle/_lifecycle.py"
agent = "qwen-35b"
timeout_s = 600
satisfies = ["E1", "E2"]
commit_message = "refactor(lifecycle): extract create.py submodule from _lifecycle.py"
prompt = """
Decompose lifecycle/_lifecycle.py by extracting create.py submodule.

1. Read src/dgov/lifecycle/_lifecycle.py completely
2. Identify all creation-related functions:
   - _refresh_codebase_md
   - _pane_lock
   - ensure_dgov_gitignored
   - _worktree_base
   - _create_worktree
   - _find_unique_slug
   - _write_worktree_instructions
   - _git_exclude_files
   - _install_worker_hooks
   - _setup_and_launch_agent
   - _pi_extension_flags
   - create_worker_pane

3. Create src/dgov/lifecycle/create.py with these functions:
   - Copy imports from _lifecycle.py (adjust for new location)
   - Copy the functions above in logical order
   - Keep internal functions prefixed with _
   - ensure_dgov_gitignored and create_worker_pane are public

4. Update src/dgov/lifecycle/__init__.py:
   - Import from create.py instead of _lifecycle.py
   - Keep metadata imports unchanged
   - Keep __all__ updated

5. DO NOT delete _lifecycle.py yet - leave it with remaining functions (cleanup, resume)

6. Run tests to verify: uv run pytest tests/test_lifecycle.py -v -m unit

7. Fix any import errors by updating imports in:
   - src/dgov/dispatch_executor.py
   - src/dgov/recovery.py
   - src/dgov/merger/_merger.py
   - src/dgov/dag_executor.py
   - src/dgov/pane_executor.py
   - src/dgov/status.py
   - src/dgov/cli/__init__.py
   - src/dgov/cli/admin.py

8. git add all modified files
9. git commit -m "refactor(lifecycle): extract create.py submodule from _lifecycle.py"
"""

[units.lifecycle-extract-create.files]
create = ["src/dgov/lifecycle/create.py"]
edit = ["src/dgov/lifecycle/__init__.py"]

[units.lifecycle-extract-cleanup]
summary = "Extract cleanup.py from lifecycle/_lifecycle.py"
agent = "qwen-35b"
timeout_s = 600
deps = ["lifecycle-extract-create"]
satisfies = ["E1", "E3"]
commit_message = "refactor(lifecycle): extract cleanup.py submodule from _lifecycle.py"
prompt = """
Decompose lifecycle/_lifecycle.py by extracting cleanup.py submodule.

1. Read current src/dgov/lifecycle/_lifecycle.py (after create.py extraction)
2. Identify all cleanup-related functions:
   - _process_snapshot
   - _collect_descendant_pids
   - _terminate_pane_process_tree
   - _full_cleanup
   - _pane_in_active_dag
   - close_worker_pane

3. Create src/dgov/lifecycle/cleanup.py with these functions:
   - Copy imports from _lifecycle.py (adjust for new location)
   - Copy the functions above
   - Keep internal functions prefixed with _
   - close_worker_pane is public

4. Update src/dgov/lifecycle/__init__.py:
   - Import from cleanup.py instead of _lifecycle.py
   - Keep existing imports from create.py and metadata
   - Update __all__ list

5. DO NOT delete _lifecycle.py yet - leave it with remaining functions (resume)

6. Run tests to verify: uv run pytest tests/test_lifecycle.py -v -m unit

7. Fix any import errors in dependent files

8. git add all modified files
9. git commit -m "refactor(lifecycle): extract cleanup.py submodule from _lifecycle.py"
"""

[units.lifecycle-extract-cleanup.files]
create = ["src/dgov/lifecycle/cleanup.py"]
edit = ["src/dgov/lifecycle/__init__.py", "src/dgov/lifecycle/_lifecycle.py"]

[units.lifecycle-extract-resume]
summary = "Extract resume.py and complete decomposition"
agent = "qwen-35b"
timeout_s = 600
deps = ["lifecycle-extract-cleanup"]
satisfies = ["E1", "E4", "E5", "E6"]
commit_message = "refactor(lifecycle): extract resume.py and delete _lifecycle.py"
prompt = """
Decompose lifecycle/_lifecycle.py by extracting resume.py submodule.

1. Read current src/dgov/lifecycle/_lifecycle.py (after create.py and cleanup.py extraction)
2. Identify remaining resume-related functions:
   - _overlay_dirty_files
   - resume_worker_pane

3. Create src/dgov/lifecycle/resume.py with these functions:
   - Copy imports from _lifecycle.py
   - Copy the two functions above
   - resume_worker_pane is public

4. Update src/dgov/lifecycle/__init__.py:
   - Import from resume.py instead of _lifecycle.py
   - Keep existing imports from create.py, cleanup.py, metadata
   - Update __all__ list

5. Delete src/dgov/lifecycle/_lifecycle.py (should now be empty of useful functions)

6. Run tests to verify: uv run pytest tests/test_lifecycle.py -v -m unit

7. Fix any import errors in dependent files

8. git add all modified files
9. git commit -m "refactor(lifecycle): extract resume.py, complete decomposition, delete _lifecycle.py"
"""

[units.lifecycle-extract-resume.files]
create = ["src/dgov/lifecycle/resume.py"]
edit = ["src/dgov/lifecycle/__init__.py"]
delete = ["src/dgov/lifecycle/_lifecycle.py"]

[units.lifecycle-final-validation]
summary = "Final validation after lifecycle decomposition"
agent = "qwen-35b"
timeout_s = 300
deps = ["lifecycle-extract-resume"]
satisfies = ["E6"]
commit_message = "chore(lifecycle): final validation after decomposition"
prompt = """
Final validation and cleanup after lifecycle decomposition.

1. Run full lifecycle test suite: uv run pytest tests/test_lifecycle.py -v
2. Run imports check: uv run python -c "from dgov.lifecycle import create_worker_pane, close_worker_pane, resume_worker_pane; print('OK')"
3. Verify no lingering _lifecycle.py imports in codebase:
   grep -rn "from dgov.lifecycle._lifecycle" src/ tests/ || echo "No lingering imports"
4. Check sentrux score improvement: uv run sentrux src/ 2>/dev/null | tail -5
5. Update CODEBASE.md if needed (regenerate: uv run dgov admin regenerate-codebase-md .)
6. If any issues found, fix and commit
7. Final git status check
"""

[units.lifecycle-final-validation.files]
edit = ["CODEBASE.md"]
