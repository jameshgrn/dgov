id = "parallel-test"
name = "Parallel Execution Test"
description = "Test that multiple agents run concurrently"

[[tasks]]
slug = "parallel-a"
agent = "mock"
prompt = "Create file parallel-a.txt with content 'A started at {time}' then wait 2 seconds then append 'A done'. Commit when complete."

[tasks.files]
create = ["parallel-a.txt"]

[[tasks]]
slug = "parallel-b"
agent = "mock"
prompt = "Create file parallel-b.txt with content 'B started at {time}' then wait 2 seconds then append 'B done'. Commit when complete."

[tasks.files]
create = ["parallel-b.txt"]

[[tasks]]
slug = "parallel-c"
agent = "mock"
prompt = "Create file parallel-c.txt with content 'C started at {time}' then wait 2 seconds then append 'C done'. Commit when complete."

[tasks.files]
create = ["parallel-c.txt"]
