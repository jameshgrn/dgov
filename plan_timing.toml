[plan]
name = "task-timing"

[tasks.track_timing]
summary = "Implement per-task timing"
prompt = """Implement per-task timing in `src/dgov/runner.py`.
1. In `EventDagRunner`, add `self._task_start_times: dict[str, float] = {}` to `__init__`.
2. In `_dispatch`, record `self._task_start_times[action.task_slug] = time.time()` just before dispatching.
3. In `_handle_worker_exit`, calculate duration and emit it in the `task_done`/`task_failed` event.
4. Update `src/dgov/cli/run.py` to collect these durations from the runner and include them in `runs.log`.
"""
commit_message = "feat: implement per-task timing in runner and log"
agent = "accounts/fireworks/routers/kimi-k2p5-turbo"
files = { edit = ["src/dgov/runner.py", "src/dgov/cli/run.py"] }
