{
  "task": "r140-recovery",
  "status": "completed",
  "commit": "9fb2012",
  "changes": [
    "src/dgov/recovery.py: Replaced raw string comparison ('done', 'active') with PaneState enum (PaneState.DONE, PaneState.ACTIVE)"
  ],
  "evals": {
    "E-no-raw-recovery": {
      "description": "regression: recovery.py uses PaneState enum, not raw strings for state comparisons",
      "evidence": "grep -n 'in (\"done\", \"active\")' src/dgov/recovery.py returns no matches"
    },
    "E-tests-pass": {
      "description": "regression: All affected unit tests still pass",
      "status": "pending_validation"
    }
  }
}
