name = "real-test-c"
description = "Test C: Add function to tests/test_other.py (different file - no conflict)"

[goal]
prompt = """Add a function to tests/test_other.py.

1. Read tests/test_other.py if it exists, otherwise create it
2. Add a function called `test_function_c()` that returns "C"
3. Save the file
4. git add tests/test_other.py
5. git commit -m "Add test_function_c"
"""
