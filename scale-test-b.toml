name = "scale-test-b"
description = "Task B: Append line to tests/scale-test.txt (same file as A)"

[goal]
prompt = """Append line B to shared file.

1. Append "Line from task B" to tests/scale-test.txt
2. git add tests/scale-test.txt
3. git commit -m "Task B: Add line to scale-test.txt"
"""
