name = "scale-test-c"
description = "Task C: Write to different file (no conflict with A or B)"

[goal]
prompt = """Write to separate file.

1. Create tests/scale-test-c.txt containing "Line from task C"
2. git add tests/scale-test-c.txt
3. git commit -m "Task C: Create separate file"
"""
