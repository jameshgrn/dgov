"""Smoke-test file A for DAG runner validation."""


def add(x: int, y: int) -> int:
    """Return x + y."""
    return x + y


def multiply(x: int, y: int) -> int:
    """Return x * y."""
    return x * y
