[plan]
name = "test-fast-dispatch"
goal = "Test the fast dispatch loop with a simple mock task"
version = 1

[units]

[units.hello]
summary = "Test fast dispatch with mock agent"
commit_message = "Add greeting from test-fast-dispatch"
files.edit = ["README.md"]
agent = "mock"
prompt = """
Write a greeting to a file.

1. Create a file called greeting.txt with content "Hello from fast dispatch!"
2. Exit with code 0
"""
